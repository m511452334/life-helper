spring:
  application:
    name: userservice
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      #server-addr: 172.22.31.124:8848 # nacos地址
      server-addr: localhost:8848 # nacos地址
      config:
        file-extension: yaml # 文件后缀名